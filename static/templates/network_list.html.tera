{% extends "nav" %}

{% block network_list %}
        <!-- Titled list -->
        <div class="card">
            <div class="center list-container">
                <h1 class="bold title-medium">WiFi Networks</h1>
                <ul class="list">
                {% if wlan_scan %}
                {% for network in wlan_scan %}
                    <li class="list-item">
                        {% if wlan_ssid == network.ssid %}
                        <a href="/network/wifi?ssid={{ wlan_ssid }}">
                            <img class="icon icon-active icon-medium list-icon" src="/icons/wifi.svg">
                            <p class="list-text">{{ network.ssid }}</p>
                            <label class="label-small list-label" title="Status">Connected</label>
                        </a>
                        {% else %}
                        <a href="/network/wifi?ssid={{ network.ssid }}">
                            <img class="icon icon-inactive icon-medium list-icon" src="/icons/wifi.svg">
                            <p class="list-text">{{ network.ssid }}</p>
                            <label class="label-small list-label" title="Status">Available</label>
                        </a>
                        {% endif %}
                    </li>
                {% endfor %}
                {% else %}
                    <li class="list-item">No networks found</li>
                {% endif %}
                </ul>
            </div>
        </div>
{% endblock %}
